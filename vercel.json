{
  "version": 2,
  "buildCommand": "cd server && pnpm install && pnpm run build",
  "builds": [
    {
      "src": "server/dist/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/dist/index.js"
    }
  ]
}
